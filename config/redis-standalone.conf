# Force Redis to run as standalone master (no replication).
# Use when Redis was previously configured as a replica and the master is unreachable.
replicaof no one
# Persistence (matches docker volume)
dir /data
